<template>
  <router-view/>
</template>

<script>
  // import {Store} from "./store";

  export default {
    mounted() {
      // console.log("token = " + Store.getters.token)
      // Store.commit('logout')
    }
  }
</script>
